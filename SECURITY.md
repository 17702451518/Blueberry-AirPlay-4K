# 安全策略

## 支持版本

| 版本 | 安全更新 |
| --- | --- |
| 3.1.x | 支持 |
| 3.0.x 及更早 | 不支持 |

## 私下报告漏洞

请使用 GitHub 仓库 **Security → Advisories → Report a vulnerability** 私下提交。请说明受影响版本、环境、复现步骤、影响与可行的缓解方式。不要在公开 Issue、截图或日志中粘贴 Token、Cookie、密码、私钥、真实 IP、设备标识或其他个人数据。

维护者会尽力确认报告、评估影响并协调修复，但本志愿项目不承诺固定响应时限。公开披露前请给维护者合理的修复窗口。

## 隐私与数据边界

本控制台没有账号系统、遥测、广告、远程更新器或录屏功能，不会读取或保存：

- API Token、Cookie、OAuth 会话、账号密码、私钥或浏览器数据；
- 投屏画面、音频内容、连接历史或运行日志；
- 旧模式参数的历史备份。

控制台只在当前用户范围写入 UxPlay 启动参数、D3D11/蓝牙发现设置，以及用户主动启用的登录启动项。具体位置见 `README.md`。

## 下载安全

- 只从本仓库 GitHub Releases 下载；
- 同时下载 `SHA256SUMS.txt` 并验证 SHA-256；
- 项目当前没有代码签名证书，未知发布者提示不等于校验通过；
- 不要为运行本项目关闭 Defender、防火墙或其他系统保护；
- 若校验值不一致，不要运行文件，请重新下载并报告。

## 发布者检查

每次发布前应检查常见密钥格式、个人绝对路径、`.env`、证书、PDB、浏览器数据库、构建缓存和历史提交。GitHub Secret scanning 不能替代人工检查。
