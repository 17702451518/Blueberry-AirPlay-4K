# 蓝莓 AirPlay 4K v3.1.1

修复开机启动设置的状态显示和操作提示。4K60、HEVC、D3D11 与接收核心版本保持不变。

## 修复内容

- 搬动或删除程序目录后，旧启动项不再因路径不匹配而被界面漏报。
- 增加“关闭本程序所有目录的开机启动”按钮，只移除本程序的 `BlueberryAirPlay4K` 登记；旧目录不存在时也可关闭。
- 明确说明开机启动运行的是后台 `uxplay-windows.exe`，而非中文控制台。
- 启用前增加二次确认，默认选择“否”。打开控制台、切换模式和手动投屏不会自动登记开机启动。
- 增加使用独立临时注册表键的 16 项回归断言。

## 下载与升级

下载 `Blueberry-AirPlay-4K-v3.1.1-win64.zip`，核对同一 Release 的 `SHA256SUMS.txt` 后完整解压。需要 Windows 10/11 x64 和 .NET 8 Desktop Runtime x64。

旧版用户请先退出控制台与接收核心，并备份原目录，再解压新版。运行 `蓝莓AirPlay4K控制台.exe`，在“启动设置”中点击“关闭本程序所有目录的开机启动”，清除旧版本留下的登记。

**升级不会擅自删除原有开机启动偏好。** 若旧登记仍存在，界面会如实显示；需要由用户点击关闭。默认关闭指没有历史登记的新环境。

包内包含 README、发行说明、开机启动说明、源码获取与第三方许可等 Markdown 文档。不要下载 GitHub 自动生成的 Source code 归档来直接运行程序。

## 验证与边界

- Release 构建通过，0 警告、0 错误；16 项启动设置回归断言通过。
- 现用目录中的新版控制台已完成启动检查；关闭启动项后，打开控制台未重新创建登记。
- 未进行整机重启验证，也未完成启用确认弹窗的交互测试。
- 本次未修改上游接收核心或画质参数，未重新进行 iPhone 全链路投屏测试。
- 4K60 是能力请求与接收端上限，实际流参数由 iOS、网络和硬件决定。程序未签名。

## 来源与许可证

控制台源码及原创图标使用 GPL-3.0-only。接收核心来自 [uxplay-windows 2.0.0.1736](https://github.com/leapbtw/uxplay-windows/releases/tag/2.0.0.1736) 与 [UxPlay 1.73.6](https://github.com/FDH2/UxPlay/releases/tag/v1.73.6)。第三方组件遵循各自许可证，包内保留上游许可及源码入口。

本项目与 Apple Inc. 无隶属、授权或赞助关系。SHA-256 是公开文件校验值，不是账号密钥。
